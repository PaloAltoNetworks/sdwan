name: initial_config_last_page_97b5e00a-7d8e-4203-b5ec-3d3c6a11244a
label: initial configuration workflow last page output
description: quick output page that workflow is complete

type: template

extends:

labels:
  collection:
    - lib

variables:
  - name: sdwan_shared_template
    description: template name for sd-wan hub sites
    default: sdwan_shared_template
    type_hint: hidden
  - name: member_pool
    description: SD-WAN VPN Address Pool
    default: 192.168.1.0/24
    type_hint: list
    help_text: Add up to 20 IP address ranges (IP network with netmask) that Panorama draws from to use as VPN tunnel IP addresses. Panorama draws from the largest range first, then from the next largest range. A VPN cluster member will get its IP address from the VPN address pool (the ranges) you provide. You must configure at least one entry.


snippets:
  - name: workflow_output
    file: workflow_output.txt